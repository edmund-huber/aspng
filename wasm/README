Build the wasm binaries (and the scaffold JS) using emscripten:

$ make bin/aspng_wasm.js

Start a webserver from git root, e.g.:

$ python3 -m http.server 8080

Now visit http://localhost:8080/wasm/ .

The default input (see wasm/input.png) is the full ripple adder.
